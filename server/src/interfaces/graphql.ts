import { resolvers } from '../../graphql/resolvers';
import { typeDefs } from '../../graphql/typesDef';

export const typeDefsInterface = typeof typeDefs;
export const resolversInterface = typeof resolvers;
